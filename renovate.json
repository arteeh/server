{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^elements/.*\\.bst$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)(?:\\s+versioning=(?<versioning>[^\\s]+))?\\n\\s*ref:\\s*['\"]?(?<currentValue>[^'\"\\s]+)['\"]?"
      ]
    }
  ],
  "homebrew": {
    "description": "Formula/*.rb pin per-arch release binaries (kc-agent_<v>_<os>_<arch>.tar.gz) with one sha256 each. Renovate's homebrew manager only understands source-archive formulae: it rewrites the url to https://github.com/<owner>/<repo>/archive/refs/tags/<tag>.tar.gz and carries over a single checksum, which leaves `bin.install \"kc-agent\"` with no binary to install and the other three architectures unresolvable. It produced exactly that in projectbluefin/server#209. Bumping these formulae means fetching four assets and four checksums, so it stays a manual change until a generator exists.",
    "enabled": false
  },
  "packageRules": [
    {
      "matchDatasources": ["git-refs"],
      "matchPackageNames": ["https://gitlab.com/freedesktop-sdk/freedesktop-sdk.git"],
      "extractVersion": "^freedesktop-sdk-(?<version>25\\.08\\.[0-9]+)$",
      "versioning": "semver"
    },
    {
      "matchDatasources": ["git-refs"],
      "matchPackageNames": ["https://gitlab.gnome.org/GNOME/gnome-build-meta.git"],
      "extractVersion": "^(?<version>50\\.[0-9]+)$",
      "versioning": "semver"
    }
  ]
}
