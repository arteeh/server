# Ensure per-device host keys exist before sshd starts. See
# ssh-host-keygen.service: keys are generated on the device, never at image
# build time, so the public release DDI carries no private key material.
[Unit]
Wants=ssh-host-keygen.service
After=ssh-host-keygen.service
