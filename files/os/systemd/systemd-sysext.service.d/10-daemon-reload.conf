# A sysext merge makes unit files appear under /usr; it does not by itself make
# systemd notice them. Without this reload, ordering edges on containerd.service
# and kubelet.service resolve against units systemd has never read, and the
# failure mode is silent: image merged, nothing running.
[Service]
ExecStartPost=/usr/bin/systemctl daemon-reload
